package io.github.wizard302.cardamom.data.settings

/** Sort order for the Tracks tab (and the Folders tab, which reuses the track list). */
enum class TrackSort { TITLE, ARTIST, ALBUM, DATE_ADDED, DURATION }

enum class AlbumSort { TITLE, ARTIST, YEAR, DATE_ADDED }

enum class ArtistSort { NAME, TRACK_COUNT }
